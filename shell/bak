#!/bin/sh

{ zip -r ~/'Documents/Vie pratique.zip' ~/'Documents/Vie pratique' >/dev/null ; printf '~/bin/bak: zip 1: code %s\n' "${?}" ; } &
{ zip -r ~/'Documents/Texts/own.zip' ~/'Documents/Texts/own' >/dev/null ; printf '~/bin/bak: zip 2: code %s\n' "${?}" ; } &

~/'Dev/drafts-and-stuff/Python/pyautogui/upload.py'
