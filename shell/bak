#!/bin/sh

#~ echo Running.

cd ~/'Documents/Texts'
{ zip -r -FS --quiet ~/'Documents/Texts/own.zip' 'own'; printf '~/bin/bak: zip Texts/own: code %s\n' "${?}" ; }

cd ~/'Documents'
{ zip -r -FS --quiet ~/'Documents/Vie pratique.zip' 'Vie pratique'; printf '~/bin/bak: zip Vie pratique: code %s\n' "${?}" ; }

#~ ~/'Dev/drafts-and-stuff/Python/pyautogui/upload.py'

#~ echo Done.
