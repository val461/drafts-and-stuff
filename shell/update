#!/bin/sh

echo '### USE XTERM ! ###'

echo 'updating mirror list with reflector'
sudo reflector --sort score --save /etc/pacman.d/mirrorlist || echo "erreur code ${?} retournée par reflector"

sudo pacman -Syyu || echo "erreur code ${?} retournée par pacman"

zsh
